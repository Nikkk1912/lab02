#include <iostream>

using namespace std;

int main() {
    char line[30];

    cout << "Enter a string: ";
    cin >> line;
    for (int i = strlen(line) - 1; i >= 0; i--) {
        cout << line[i];
    }
    
}
